# NOMI 重塑 Loop 工作区(notch · Swift)

阶段 4B「notch NOMI 重塑」loop 的专属工作区,结构同 ../nomi/(log.md / reports/ / shots/ / backlog.md,自行扩展)。

设计基线(只读):docs/design/helm-notch-nomi.html —— 单体生长 morph/折叠态(左组—摄像头空档—右组)/
顶行(左 logo+Helm 右天气+设置)/底部圆钮 dock 5 模块/智能体上下滑子页(会话/端口/PR)/
横扫切模块/banner 单体化/深默认+浅色。
硬门:swift build && swift test 绿才 commit(feat/notch-nomi-*);快照视觉对比;CI 过后合并留用户。
旧基线 notch/docs/helm-notch-pro.html 自此存档(实现切换完成前,现网行为仍以旧稿为准)。
