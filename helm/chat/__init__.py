"""Chat & multi-model (F2): provider management + streaming chat. m1 ships the
provider data model + management REST; adapters (m2), sessions/messages +
streaming (m3) and the UI (m4) follow. Multi-model compare + vision are P1.
"""
