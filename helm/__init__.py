"""Helm — local-first personal AI workspace (coding cockpit + AI brain).

platform-shell is the foundation every feature room builds on: the Python
backend runtime, local storage, secret/settings, the desktop shell, and
packaging. Per recorded decision, Helm starts from a fresh minimal skeleton and
pulls Odysseus modules in per feature room rather than vendoring the whole tree.
"""

__version__ = "0.0.1"
