# Helm backend runtime dependencies (platform-shell foundation).
# Pulled-in Odysseus modules will add their own deps per feature room.
fastapi
uvicorn[standard]
SQLAlchemy>=2.0
cryptography
python-multipart
watchdog
httpx
# memory-rag-skills: vector store + embedder (constraint 8ace2b3a, Odysseus stack)
chromadb
fastembed
# memory-rag-skills m4: RAG document extraction (graceful if absent)
pypdf
python-docx
# memory-rag-skills m6: MCP server (expose memory/RAG to terminal agent)
mcp
# journal-notes-tasks m4: scheduled-task cron parsing
croniter
